# MySQL

# 目录

# ~~字段相关~~

## 字段类型

```mysql
INT(11)
VARCHAR(50)  -- 不浪费空间
CHAR(50)  -- 不满50字符串会填充空格至50字符串，浪费空间
```

## 字段相关

```mysql
PK  -- Primary Key，主键，可以有双主键(单个可重复，配对结果不重复)
NN  -- NOT NULL，不为空
AI  -- AUTO_Increment，自动增加
Default/Expression  -- 默认值
```

