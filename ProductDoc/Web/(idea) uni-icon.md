# (idea) uni-icon

现存的在线图标库非常多，但是存在一些问题：

引用便捷度（在一些前端UI库中该问题得到了解决，可以通过一个非常短的 “icon名” 就能够直接使用，开盒即用，非常方便）

但是一般需要安装指定UI库的环境依赖，如果不想要这个依赖，直接通过url来引用的话，url较长



我就想，能不能有一个统一的ui库，其管理方式更像是npm这种。大家均可上传。然后各种服务器也可互相镜像。

此时访问某个图标，都有一个与域名无关（镜像源可以被替换）的icon名，例如：`icon:ant/code_200`





