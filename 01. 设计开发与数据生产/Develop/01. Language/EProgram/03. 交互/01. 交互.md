# 易语言

# 目录

# 字节集与磁盘

## 字节集

字节集，就是字节的集合，字节集相当于一个字节型的数组

万物皆字符集，文件、图片等由字符集组成，exe记事本打开也为乱码

## 所有内置操作

### 文本操作

- `取文本长度`
- `画文本左边`，（字符串，取多少个**字节**）
- `取文本右边`，（字符串，取多少个**字节**），不会调转顺序
- `取文本中间`，（字符串，起始，**字节**数目）
- `字符`
- `取代码`
- `寻找文本`，（返回第一次出现的序列），如名
- `倒找文本`
- `到大写`
- `到小写`
- `到全角`
- `到半角`
- `到文本`
- `删首空`
- `删尾空`
- `删首尾空`
- `删全部空`
- `文本替换`
- `子文本替换`
- `取空白文本`
- `取重复文本`
- `文本比较`
- `分割文本`
- `指针到文本`
- `支本到UTF8`
- `UTF8到文本`

### 字符集操作

- `取字节集长度`
- `到字节集`，类型转换为字节集
- `取字节集数据`
- `取字节集左边`
- `取字节集右边`
- `取令节集中间`
- `寻找字节集`
- `倒找字节集`
- `字节集替换`
- `子节集替换`，如名
- `取空白字节集`
- `取重复李节集`
- `分割字节集`
- `指针到字节集`
- `指针到整数`
- `指针到小数`
- `指针到双精度小数`
- `取字节集内整数`
- `取字节集内整数`

### 磁盘操作

- `取磁盘总空间`

- `取磁盘剩余空间`

- `取磁盘卷标`

- `置磁盘卷标`

- `改变驱动器`

- `改变目录`，`取当前目录`所表示的目录

- `取当前目录`，返回字符串，可以被`改变目录`改变。而`取运行目录`（归类于环境存取）不可用被改变

- `创建自录`

- `删除目录`

- `复制文件`

- `移动文件`

- `删除文件`

- `文件更名`

- `文件是否存在`

- `寻找文件`，返回的是文件名。通常配合通配符`*`使用，但只会匹配第一个，需要循环使用该命令以遍历所有匹配项（第二次匹配不用给参数）

    - ```易
        文件 = 寻找文件（“C:\*”，[#子目录]） ’ 子目录参数可选，搜索文件夹还是文件的区别
        调试输出（文件）
        	判断循环首（真）
        	文件 = 寻找文件（，[#子目录]）
        		如果真（文件=’‘）
        		跳出循环（）
        	调试输出（文件）
        判断循环尾（）
        ```

- `取文件时间`

- `取文件尺寸`

- `取文件属性`

- `置文件属性`

- `取临时文件名`

- `读入文件`，返回字节集类型（不是，为什么分类在磁盘操作而不是文件读写）

- `写到文件`

### 文件读写

- `打开文件`
- `打并内存文件`
- `关闭文件`
- `关闭所有文件`
- `锁住文件`
- `解锁文件`
- `移动读写位置`
- `移到文件首`
- `移到文件尾`
- `读入字节集`
- `写出字节集`
- `读入文本`
- `写出文本`
- `读入一行`
- `写文本行`
- `读入数据`
- `写出数据`
- `是否在文件尾`
- `取读写位置`
- `取文件长读`
- `插入字节集`
- `插入文本`
- `插入文本行`
- `删除数据`
- `打开加密文件`



















